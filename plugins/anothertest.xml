<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>
<!-- Saved on Saturday, November 04, 2017, 7:55 AM -->
<!-- MuClient version 4.98 -->

<!-- Plugin "anothertest" generated by Plugin Wizard -->

<muclient>
<plugin
   name="anothertest"
   author="Gloominati"
   id="b475d8031b2991c053bf1854"
   language="PhpScript"
   purpose="again"
   date_written="2017-11-04 07:54:09"
   requires="4.98"
   version="1.0"
   >

</plugin>


<!--  Get our standard constants -->

<include name="constants.php"/>

<!--  Aliases  -->

<aliases>
  <alias
   match="#alias {*} {*}"
   enabled="y"
   group="special_convenience_aliases"
   send_to="12"
   ignore_case="y"
   sequence="100"
  >
  <send>require "addxml"  -- addxml extension

-- add the alias

addxml.alias {
  name = "command_line_alias_" .. string.gsub(Base64Encode("%1"), "=", ""), 
  match = "%1",
  send = "%2",
  sequence = 100,
  enabled = true,
  send_to = 10,
  group = "command_line_aliases"
             }

ColourNote ("white", "green", "Added alias to match on '%1', sending '%2'")
  </send>
  </alias>
  <alias
   match="#trigger {*} {*}"
   enabled="y"
   group="special_convenience_aliases"
   send_to="12"
   ignore_case="y"
   sequence="100"
  >
  <send>require "addxml"  -- addxml extension

-- add the trigger 

addxml.trigger {
  name = "command_line_trigger_" .. string.gsub(Base64Encode("%1"), "=", ""), 
  match = "%1", 
  send = "%2",
  sequence = 100,
  enabled = true,
  send_to = 10,
  group = "command_line_triggers"
               }

ColourNote ("white", "green", "Added trigger to match on '%1', sending '%2'")
  </send>
  </alias>
  <alias
   script="reset_my_shit"
   match="reset"
   enabled="y"
   sequence="100"
  >
  <send>&lt;script&gt;reset_my_shit () world.ReloadPlugin "SpellsCombat"end&lt;/script&gt;</send>
  </alias>
</aliases>

</muclient>
